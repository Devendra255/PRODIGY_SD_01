# Simple Temperature converter With GUI

A simple Temperature converter created for Prodigy internship task.

This is created in Python, and it uses *customtkinter* module for GUI.

To run this program, you need to first install *customtkinter*.

So in terminal/CMD type 
```
pip install customtkinter
```
then run this program by doing
```
python temp_convert.py
```
